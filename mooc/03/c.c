#include <stdio.h>
int main()
{

    // const int YIBAI =100;  // 定义一个常量 一旦定义不允许修改

    int a = 0;
    int b = 0;
    scanf("%d %d", &a, &b);   // %d %d 输入的内容 一定要全部匹配 

    printf("%d %d\n", a, b);
    // printf("%d",YIBAI);

    return 0;
}